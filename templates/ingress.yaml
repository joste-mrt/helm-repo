{{- if .Values.ingress.enabled  }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mrt-ingress-test
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: helm.mrtinternal
    http:
      paths:
      - path: /foo
        backend:
          serviceName: http
          servicePort: 3000
      - path: /bar
        backend:
          serviceName: ssh
          servicePort: 22
{{- end -}}
